# 简单待办清单

## 项目名称

简单待办清单

## 目标用户

这个项目面向想要记录日常小任务的人，也适合刚开始学习做网页小工具的新手。

用户不需要懂复杂的软件功能，只需要知道自己有哪些事情要做，就可以使用这个工具。比如学生、上班族、自由职业者，或者任何想把事情简单记下来的人。

## 使用场景

用户可以在一天开始前，把今天要做的事情写进待办清单里。

在完成一件事情后，用户可以把它勾选为已完成。这样可以清楚看到哪些事情已经做完，哪些事情还没有完成。

如果某个待办事项不再需要，用户可以直接删除它，让列表保持干净。

常见场景包括：

- 记录今天要完成的学习任务
- 记录工作中要处理的小事项
- 记录购物、整理、打电话等生活事项
- 临时记下不想忘记的事情

## 核心功能

当前版本只保留最基础、最容易理解的四个功能：

- 添加待办事项
- 显示待办列表
- 勾选完成
- 删除待办事项

这些功能可以帮助用户完成一个完整的待办清单流程：写下来、看列表、标记完成、清理不需要的事项。

## 页面草图

页面可以从上到下分成几个简单区域。新手可以先把它想象成一张纸：最上面写标题，中间用来输入新任务，下面显示已经添加的任务。

页面结构如下：

1. 页面标题

   页面最上方显示标题“简单待办清单”。用户一打开页面，就能知道这个工具是用来记录待办事项的。

2. 输入框

   标题下面放一个输入框。用户可以在输入框里写下自己要做的事情，比如“完成作业”或“买牛奶”。

3. 添加按钮

   输入框旁边放一个“添加”按钮。用户写好任务后，点击这个按钮，就可以把这条任务加入待办事项列表。

4. 待办事项列表

   输入框和添加按钮下面显示待办事项列表。每添加一条任务，列表里就多出一条内容。用户可以在这里看到自己目前要做的所有事情。

5. 每一条待办事项

   列表里的每一条待办事项都包含三个部分：

   - 勾选按钮：放在任务左边。用户完成这件事后，可以点击它，把任务标记为已完成。
   - 任务文字：放在中间。这里显示用户输入的待办内容。
   - 删除按钮：放在任务右边。用户不再需要这条任务时，可以点击它，把这条任务从列表中删除。

一个最简单的页面可以理解为：

```text
简单待办清单

[输入一件要做的事] [添加]

[ ] 完成作业        [删除]
[x] 买牛奶          [删除]
[ ] 整理书桌        [删除]
```

这个草图只说明页面需要有哪些内容，不要求一开始就做得很复杂。先让用户能看懂、会使用，就是当前阶段最重要的目标。

## 当前完成情况

目前项目已经完成了一个可以使用的简单版本。

已经完成的内容包括：

- 创建了 `index.html` 页面
- 页面可以显示标题、输入框、添加按钮和待办列表
- 用户可以添加新的待办事项
- 用户可以删除某一条待办事项
- 用户可以勾选完成，也可以取消完成
- 用户可以按 Enter 添加待办事项
- 使用 localStorage 保存待办事项，刷新页面后仍然保留

当前版本已经覆盖了简单待办清单最基础的使用流程：打开页面、查看列表、添加任务、完成任务、删除任务，并且能在刷新后保留数据。

## 暂时不做什么

为了让项目保持简单，当前阶段暂时不做以下内容：

- 不做账号登录
- 不做多人协作
- 不做日期提醒
- 不做任务分类
- 不做优先级设置
- 不做搜索功能
- 不做复杂的数据统计
- 不接入支付或会员功能

这些功能以后可以根据需要再考虑。现在最重要的是先把基础待办清单做清楚。

## V2 计划

V2 的目标是：让简单待办清单更清楚、更好看、更容易使用。

V2 暂定只做这些改进：

- 优化页面整体布局，让内容居中、间距更舒服
- 美化标题、输入框、添加按钮和待办事项卡片
- 让已完成的待办事项更明显，比如文字变淡、出现删除线
- 让删除按钮更容易看懂
- 在列表为空时显示一句提示，比如“还没有待办事项”
- 保持代码简单，适合新手继续学习

V2 暂时不做这些内容：

- 不做登录
- 不做多人协作
- 不做复杂分类
- 不做日期提醒
- 不做后端服务器

V2 仍然是一个小而清楚的版本。它不会把项目变成复杂的任务管理系统，而是在 V1 的基础上，让页面体验更舒服，也让新手更容易理解代码是怎样一步步变好的。

## V2 当前完成情况

目前 V2 已经完成了几个让页面更清楚、更好用的小改进。

V2 已经完成的内容包括：

- 页面样式优化
- 空列表提示
- 未完成数量统计
- 一键清除已完成

原有 V1 功能仍然保留：

- 添加待办事项
- 删除待办事项
- 勾选完成和取消完成
- 按 Enter 添加待办事项
- 使用 localStorage 保存待办事项，刷新页面后仍然保留

V2 仍然没有做这些内容：

- 没有做登录
- 没有做多人协作
- 没有做后端服务器

当前项目仍然是一个本地浏览器小工具。用户可以直接打开 `index.html` 使用，不需要账号，也不需要连接服务器。

## V3 计划

V3 的目标是：增加任务筛选功能，让用户可以按状态查看待办事项。

V3 暂定只做这些改进：

- 增加三个筛选选项：全部、未完成、已完成
- 点击“全部”时显示所有待办事项
- 点击“未完成”时只显示没有勾选的事项
- 点击“已完成”时只显示已经勾选的事项
- 筛选只改变页面显示，不删除任何任务
- 保留 V1 和 V2 已有功能

V3 暂时不做这些内容：

- 不做搜索
- 不做分类
- 不做日期提醒
- 不做账号登录
- 不做后端服务器

V3 仍然会保持简单。它只是在现有待办清单基础上增加查看方式，让用户更容易找到自己想看的任务。

## V3 当前完成情况

目前 V3 已经完成了任务筛选功能。

V3 已经完成的内容包括：

- 增加筛选按钮：全部、未完成、已完成
- 点击“全部”显示所有待办事项
- 点击“未完成”只显示未完成事项
- 点击“已完成”只显示已完成事项
- 筛选只改变显示，不删除任务
- 当前选中的筛选按钮有明显样式
- V1 和 V2 的已有功能仍然保留

这个版本让用户可以更方便地查看不同状态的待办事项，同时不会改变或删除原来的任务数据。

## V4 计划

V4 的目标是：增加编辑待办事项文字的功能，让用户可以修改已经添加的任务内容。

V4 暂定只做这些改进：

- 每条待办事项可以进入编辑状态
- 用户可以修改任务文字
- 用户可以保存修改
- 用户可以取消编辑
- 编辑后要自动保存到 localStorage
- 保留 V1、V2、V3 已有功能

V4 暂时不做这些内容：

- 不做复杂富文本编辑
- 不做编辑历史记录
- 不做多人协作
- 不做账号登录
- 不做后端服务器

V4 仍然会保持简单。它只是在已有待办事项上增加“改文字”的能力，让用户发现写错或想调整任务内容时，可以直接修改，不需要先删除再重新添加。

## V4 当前完成情况

目前 V4 已经完成了编辑待办事项文字的功能。

V4 已经完成的内容包括：

- 每条待办事项可以编辑文字
- 点击“编辑”进入编辑状态
- 编辑状态里可以修改任务文字
- 点击“保存”后更新任务文字
- 点击“取消”后保留原文字
- 编辑后的内容会保存到 localStorage
- V1、V2、V3 的已有功能仍然保留

V4 仍然没有做这些内容：

- 没有做复杂富文本编辑
- 没有做编辑历史记录
- 没有做多人协作
- 没有做账号登录
- 没有做后端服务器

当前编辑功能只处理普通文字，保持简单直接，适合新手继续学习。

## V5 计划

V5 的目标是：给每条待办事项增加创建时间，让用户知道任务是什么时候添加的。

V5 暂定只做这些改进：

- 新增待办事项时，自动记录创建时间
- 每条待办事项显示创建时间
- 创建时间用简单格式显示，比如：创建于 14:32
- 刷新页面后，创建时间仍然保留
- 编辑任务文字时，不改变创建时间
- 保留 V1 到 V4 已有功能

V5 暂时不做这些内容：

- 不做截止日期
- 不做提醒通知
- 不做日历
- 不做按时间排序
- 不做后端服务器

V5 仍然保持轻量。它只记录任务被添加的时间，不把待办清单变成复杂的日程或提醒工具。

## V5 当前完成情况

目前 V5 已经完成了创建时间功能。

V5 已经完成的内容包括：

- 新增待办事项时会自动记录创建时间
- 每条待办事项会显示创建时间
- 创建时间格式简单，比如“创建于 14:32”
- 刷新页面后创建时间仍然保留
- 编辑任务文字时不会改变创建时间
- 旧数据如果没有创建时间，也能正常显示，不会报错
- V1 到 V4 的已有功能仍然保留

V5 仍然没有做这些内容：

- 没有做截止日期
- 没有做提醒通知
- 没有做日历
- 没有做按时间排序
- 没有做后端服务器

当前创建时间只是帮助用户知道任务是什么时候添加的，不会影响任务顺序，也不会触发提醒。

## V6 计划

V6 的目标是：给待办事项增加截止日期，让用户可以知道任务最好什么时候完成。

V6 暂定只做这些改进：

- 添加任务时，可以选择一个截止日期
- 每条任务可以显示截止日期
- 截止日期可以为空，也就是不是每条任务都必须有日期
- 编辑任务时，可以修改截止日期
- 刷新页面后，截止日期仍然保留
- 保留 V1 到 V5 已有功能

V6 暂时不做这些内容：

- 不做自动提醒通知
- 不做日历视图
- 不做按日期排序
- 不做重复任务
- 不做后端服务器

V6 仍然保持轻量。它只是在任务上增加一个日期信息，不会把项目变成复杂的日程管理系统。

## V6 当前完成情况

目前 V6 已经完成了截止日期功能。

V6 已经完成的内容包括：

- 添加待办事项时可以选择截止日期
- 每条待办事项会显示截止日期
- 没有选择截止日期时，会显示“截止：未设置”
- 编辑待办事项时可以修改截止日期
- 编辑任务文字时不会改变创建时间
- 刷新页面后，截止日期仍然保留
- V1 到 V5 的已有功能仍然保留

V6 仍然没有做这些内容：

- 没有做自动提醒通知
- 没有做日历视图
- 没有做按日期排序
- 没有做重复任务
- 没有做后端服务器

当前截止日期只是帮助用户记录任务最好什么时候完成，不会自动提醒，也不会改变任务排序。

## V7 计划

V7 的目标是：增强日期管理，让用户更容易看出哪些任务更着急。

V7 暂定做一组相关改进：

- 增加排序方式，可以按截止日期优先显示任务
- 没有截止日期的任务在按日期排序时放在后面
- 今天截止的未完成任务显示“今天截止”
- 已经逾期的未完成任务显示“已逾期”
- 在页面上显示日期统计，比如今天截止几件、已逾期几件
- 保留 V1 到 V6 已有功能

V7 暂时不做这些内容：

- 不做自动提醒通知
- 不做日历视图
- 不做拖拽排序
- 不做按优先级排序
- 不做后端服务器

V7 会比前几个版本更完整一些。它不是只增加一个按钮，而是围绕“日期管理”做一组小改进。

## V7 当前完成情况

目前 V7 已经完成了日期管理增强。

V7 已经完成的内容包括：

- 增加排序方式选择
- 支持“原来的顺序”
- 支持“截止日期优先”
- 按截止日期排序时，没有日期的任务会排在后面
- 未完成任务如果今天截止，会显示“今天截止”
- 未完成任务如果已经过期，会显示“已逾期”
- 页面会显示“今天截止 X 件，已逾期 X 件”
- V1 到 V6 的已有功能仍然保留

V7 仍然没有做这些内容：

- 没有做自动提醒通知
- 没有做日历视图
- 没有做拖拽排序
- 没有做按优先级排序
- 没有做后端服务器

当前版本让待办清单开始具备更清楚的时间管理能力，但仍然保持为一个本地浏览器小工具。

## V8 计划

V8 的目标是：增强输入和操作反馈，让用户更清楚自己刚才做了什么，也减少误操作。

V8 暂定做一组相关改进：

- 空输入时显示提示，不再只是没有反应
- 限制任务文字长度，避免一条任务太长影响页面
- 显示当前输入字数，让用户知道还能输入多少
- 添加成功后显示短提示
- 编辑保存成功后显示短提示
- localStorage 保存失败时显示兜底提示
- 清除已完成前先让用户确认，避免误删
- 保留 V1 到 V7 已有功能

V8 暂时不做这些内容：

- 不做复杂弹窗系统
- 不做撤销删除
- 不做批量编辑
- 不做账号登录
- 不做后端服务器

V8 重点不是增加任务管理能力，而是让已有操作更稳、更清楚。

## V8 当前完成情况

目前 V8 已经完成了输入体验增强。

V8 已经完成的内容包括：

- 空输入时会提示“请先输入一件要做的事”
- 任务文字限制为最多 60 字
- 输入框下方会显示当前字数
- 接近字数上限时，字数提示会变成提醒颜色
- 添加成功后会显示短提示
- 编辑保存成功后会显示短提示
- localStorage 保存失败时会显示兜底提示
- 点击“清除已完成”前会弹出确认
- 如果没有已完成任务，点击“清除已完成”会显示提示
- V1 到 V7 的已有功能仍然保留

V8 仍然没有做这些内容：

- 没有做复杂弹窗系统
- 没有做撤销删除
- 没有做批量编辑
- 没有做账号登录
- 没有做后端服务器

当前版本让待办清单在输入、保存和清理任务时更容易理解，也更不容易误操作。

## V9 计划

V9 的目标是：增强批量操作能力，让用户可以一次处理多条任务。

V9 暂定做一组相关改进：

- 增加“一键全部完成”
- 增加“一键全部取消完成”
- 显示总任务数、已完成数、未完成数
- 当没有任何任务时，批量操作按钮不可用
- 批量操作完成后显示短提示
- 保留 V1 到 V8 已有功能

V9 暂时不做这些内容：

- 不做批量删除未完成任务
- 不做批量编辑文字
- 不做拖拽多选
- 不做账号登录
- 不做后端服务器

V9 重点是让用户能更快处理整批任务，而不是一条一条重复点击。

## V9 当前完成情况

目前 V9 已经完成了批量操作增强。

V9 已经完成的内容包括：

- 增加“全部完成”按钮
- 增加“全部取消完成”按钮
- 原来的“清除已完成”仍然保留
- 数量统计升级为“共 X 件，已完成 X 件，未完成 X 件”
- 没有任何任务时，批量操作按钮会自动不可用
- 点击“全部完成”后，会把所有未完成事项设为完成
- 点击“全部取消完成”后，会把所有已完成事项恢复为未完成
- 批量操作后会显示短提示
- 批量操作后会自动保存到 localStorage
- 批量操作后会自动更新筛选、排序、数量统计和日期统计
- V1 到 V8 的已有功能仍然保留

V9 仍然没有做这些内容：

- 没有做批量删除未完成任务
- 没有做批量编辑文字
- 没有做拖拽多选
- 没有做账号登录
- 没有做后端服务器

当前版本让待办清单更适合处理多条任务，也让用户可以更快完成整理。

## V10 计划

V10 的目标是：增加搜索与快速定位能力，让用户在任务变多时也能快速找到想看的事项。

V10 暂定做一组相关改进：

- 增加搜索框
- 输入关键词后，只显示匹配的待办事项
- 搜索可以和“全部 / 未完成 / 已完成”筛选一起工作
- 搜索可以和排序方式一起工作
- 显示当前搜索结果数量
- 增加“清空搜索”按钮
- 搜索不到结果时显示清楚提示
- 保留 V1 到 V9 已有功能

V10 暂时不做这些内容：

- 不搜索创建时间
- 不搜索截止日期
- 不做高级搜索语法
- 不做标签系统
- 不做后端服务器

V10 重点是让用户在任务数量变多时，可以更快定位任务，而不用从列表里一条条找。

## V10 当前完成情况

目前 V10 已经完成了搜索与快速定位增强。

V10 已经完成的内容包括：

- 增加搜索输入框
- 输入关键词后，会只显示任务文字匹配的事项
- 搜索会和状态筛选一起工作
- 搜索会和截止日期排序一起工作
- 搜索时会显示“找到 X 条结果”
- 增加“清空搜索”按钮
- 没有搜索内容时，“清空搜索”按钮不可用
- 搜索不到结果时，会显示“没有找到匹配的待办事项”
- V1 到 V9 的已有功能仍然保留

V10 仍然没有做这些内容：

- 没有搜索创建时间
- 没有搜索截止日期
- 没有做高级搜索语法
- 没有做标签系统
- 没有做后端服务器

当前版本让待办清单在任务较多时更容易使用，也开始具备轻量搜索能力。

## V11 计划

V11 的目标是：把待办清单升级成更完整的任务管理增强版，一次推进多个相关方向。

V11 暂定做三组改进：

第一组：优先级管理

- 添加任务时可以选择优先级：低、中、高
- 每条任务显示优先级
- 高优先级用更明显的颜色
- 编辑任务时可以修改优先级
- 增加“优先级优先”排序方式

第二组：任务备注

- 添加任务时可以写备注
- 每条任务可以显示备注
- 没有备注时不显示备注区域
- 编辑任务时可以修改备注
- 搜索时可以匹配任务文字和备注

第三组：界面整理

- 把添加任务区域整理成独立区域
- 把统计、批量操作、筛选、搜索、排序整理成管理区域
- 让输入区和管理区更清楚
- 保持手机屏幕上也能正常使用
- 保留 V1 到 V10 已有功能

V11 暂时不做这些内容：

- 不做标签系统
- 不做项目分组
- 不做拖拽排序
- 不做账号登录
- 不做后端服务器

V11 开始从“单点功能升级”进入“多组相关能力一起升级”的节奏。

## V11 当前完成情况

目前 V11 已经完成了任务管理增强。

V11 已经完成的内容包括：

- 添加任务时可以选择低、中、高优先级
- 每条任务会显示优先级标记
- 高、中、低优先级有不同颜色
- 编辑任务时可以修改优先级
- 排序方式新增“优先级优先”
- 添加任务时可以填写备注
- 每条任务可以显示备注
- 没有备注时不会显示空备注区域
- 编辑任务时可以修改备注
- 搜索会同时匹配任务文字和备注
- 添加区域被整理成独立的输入区域
- 管理区域集中放置统计、批量操作、筛选、搜索和排序
- 手机屏幕上仍然可以正常使用
- V1 到 V10 的已有功能仍然保留

V11 仍然没有做这些内容：

- 没有做标签系统
- 没有做项目分组
- 没有做拖拽排序
- 没有做账号登录
- 没有做后端服务器

当前版本一次增加了多个相关能力，让待办清单更接近真正的轻量任务管理工具。

## V12 计划

V12 的目标是：让待办清单开始具备更清楚的分类能力和更安全的数据管理能力。

V12 暂定做三组改进：

第一组：标签管理

- 添加任务时可以填写标签，比如“学习”“工作”“生活”
- 每条任务显示自己的标签
- 编辑任务时可以修改标签
- 搜索时可以匹配任务文字、备注和标签
- 可以按标签筛选任务

第二组：快速日期

- 添加任务时可以一键把截止日期设为今天
- 添加任务时可以一键把截止日期设为明天
- 可以一键清除添加区的截止日期
- 保留手动选择截止日期的方式

第三组：数据管理

- 页面显示浏览器当前保存了多少条任务
- 增加“清空全部”按钮
- 清空全部前需要二次确认，避免误删
- 清空全部后，标签筛选会自动回到“全部标签”
- 保留 V1 到 V11 已有功能

V12 暂时不做这些内容：

- 不做项目分组
- 不做拖拽排序
- 不做撤销删除
- 不做账号登录
- 不做后端服务器

V12 的重点是让任务可以按标签整理，并开始加入更完整的数据管理意识。

## V12 当前完成情况

目前 V12 已经完成了标签、快速日期和数据管理升级。

V12 已经完成的内容包括：

- 添加任务时可以填写标签
- 每条任务会显示标签
- 编辑任务时可以修改标签
- 标签会自动去掉前后多余空格
- 搜索会同时匹配任务文字、备注和标签
- 管理区域新增“标签筛选”
- 标签筛选会自动根据已有任务生成标签选项
- 添加区新增“今天”“明天”“清除日期”三个快速日期按钮
- 页面会显示浏览器当前保存了多少条任务
- 新增“清空全部”按钮
- 点击“清空全部”前需要二次确认
- 清空全部后会重置标签筛选
- V1 到 V11 的已有功能仍然保留

V12 仍然没有做这些内容：

- 没有做项目分组
- 没有做拖拽排序
- 没有做撤销删除
- 没有做账号登录
- 没有做后端服务器

当前版本已经从“任务管理增强版”继续升级为“带分类和数据管理意识的本地待办工具”。

## V13 计划

V13 的目标是：增强数据安全，让用户不容易因为误删或浏览器问题丢失任务。

V13 暂定做四组改进：

第一组：撤销删除

- 删除单条任务后，可以撤销
- 清除已完成后，可以撤销
- 清空全部后，可以撤销
- 撤销提示只保留一段时间，避免页面一直堆提示

第二组：导出备份

- 可以把当前任务导出为 JSON 备份文件
- 备份文件包含任务文字、完成状态、创建时间、截止日期、优先级、标签和备注
- 没有任务时，导出按钮不可用

第三组：导入备份

- 可以选择以前导出的 JSON 备份文件
- 导入前会检查文件里是否有有效任务
- 导入会替换当前列表，所以会先确认
- 导入后会重新保存到浏览器

第四组：恢复示例数据

- 可以一键恢复三条示例任务
- 恢复示例数据会替换当前列表，所以会先确认
- 恢复后筛选、搜索和排序会回到默认状态
- 保留 V1 到 V12 已有功能

V13 暂时不做这些内容：

- 不做云端同步
- 不做账号登录
- 不做自动备份
- 不做复杂文件格式
- 不做后端服务器

V13 的重点是让这个本地小工具具备基本的数据保护意识。

## V13 当前完成情况

目前 V13 已经完成了数据安全和备份升级。

V13 已经完成的内容包括：

- 删除单条任务后，会显示可撤销提示
- 清除已完成任务后，可以撤销
- 清空全部任务后，可以撤销
- 撤销成功后，会恢复被删除的任务
- 撤销提示会在一段时间后自动隐藏
- 新增“导出备份”按钮
- 导出备份会生成 JSON 文件
- 导出的文件包含当前任务数据
- 没有任务时，“导出备份”按钮不可用
- 新增“导入备份”入口
- 导入时会读取 JSON 文件
- 导入前会检查文件中是否有有效任务
- 导入会替换当前列表，所以会先确认
- 新增“恢复示例数据”按钮
- 恢复示例数据会替换当前列表，所以会先确认
- 恢复示例数据后，筛选、搜索和排序会回到默认状态
- V1 到 V12 的已有功能仍然保留

V13 仍然没有做这些内容：

- 没有做云端同步
- 没有做账号登录
- 没有做自动备份
- 没有做复杂文件格式
- 没有做后端服务器

当前版本让待办清单从“本地任务工具”继续升级为“有基础备份和误删保护的本地任务工具”。

## V14 计划

V14 的目标是：整理项目结构，让后续继续开发更清楚、更容易维护。

V14 暂定做三组改进：

第一组：拆分文件

- 把 `index.html` 里的样式拆到 `style.css`
- 把 `index.html` 里的功能代码拆到 `script.js`
- `index.html` 主要保留页面结构
- 页面仍然可以通过双击 `index.html` 打开

第二组：保持功能不变

- 保留 V1 到 V13 的所有功能
- 拆分文件后，添加、删除、筛选、搜索、排序、标签、备份等功能仍然可用
- 不改变用户看到的主要界面
- 不增加新业务功能，优先保证稳定

第三组：更新文档

- 更新 `README.md` 的当前版本和文件说明
- 更新 `LEARNING.md`，解释为什么要拆分 HTML、CSS、JavaScript
- 让新手能看懂三个文件分别负责什么

V14 暂时不做这些内容：

- 不做新任务功能
- 不做云端同步
- 不做账号登录
- 不做后端服务器
- 不发布网站

V14 的重点是从“功能堆在一个文件里”升级为“项目开始有清楚结构”。

## V14 当前完成情况

目前 V14 已经完成了项目结构整理。

V14 已经完成的内容包括：

- 新增 `style.css`
- 新增 `script.js`
- `index.html` 已改为引用 `style.css`
- `index.html` 已改为引用 `script.js`
- 原来的页面结构保留在 `index.html`
- 原来的页面样式已迁移到 `style.css`
- 原来的待办功能代码已迁移到 `script.js`
- V1 到 V13 的已有功能仍然保留
- 页面仍然可以通过双击 `index.html` 打开
- `README.md` 已记录 V14
- `LEARNING.md` 已记录 V14

V14 仍然没有做这些内容：

- 没有增加新任务功能
- 没有做云端同步
- 没有做账号登录
- 没有做后端服务器
- 没有发布网站

当前版本让项目从“单文件网页”升级为“结构更清楚的本地网页项目”。

## V15 计划

V15 的目标是：整理 `script.js` 的代码可读性，让后续继续开发更容易。

V15 暂定做四组改进：

第一组：增加代码分区

- 把 `script.js` 分成清楚的区域
- 标出页面元素、配置状态、初始数据、保存提示、数据工具、筛选排序、页面渲染、用户操作、事件绑定、页面启动
- 让新手打开代码时知道每一段大概负责什么

第二组：整理重复逻辑

- 把“保存数据并刷新页面”整理成小函数
- 把“清空添加表单”整理成小函数
- 把“找出被删除的任务”整理成小函数
- 把“删除单条任务”整理成小函数
- 把“保存编辑结果”整理成小函数
- 把“快速设置日期”整理成小函数

第三组：补充关键注释

- 在重要区域加新手能理解的注释
- 解释 `renderTodos()` 为什么是页面渲染核心
- 解释筛选、搜索、标签筛选的处理顺序
- 解释事件绑定是把用户操作连接到函数

第四组：保持功能稳定

- 不改变页面主要外观
- 不删除 V1 到 V14 的已有功能
- 不改用户已有数据格式
- 让整理代码后，原功能仍然正常

V15 暂时不做这些内容：

- 不增加新业务功能
- 不重新设计页面
- 不做复杂工程框架
- 不做云端同步
- 不做后端服务器

V15 的重点是让项目从“能跑”继续升级为“更容易读、更容易改”。

## V15 当前完成情况

目前 V15 已经完成了 `script.js` 的可读性整理。

V15 已经完成的内容包括：

- `script.js` 增加了清楚的分区注释
- 页面元素区域已标出
- 固定配置和页面状态区域已标出
- 初始数据区域已标出
- 保存与提示区域已标出
- 数据小工具区域已标出
- 统计、筛选、搜索、排序区域已标出
- 页面渲染区域已标出
- 用户操作区域已标出
- 事件绑定区域已标出
- 页面启动区域已标出
- 新增 `backupVersion` 配置，备份版本号不再散落在功能代码里
- 新增 `saveAndRender()`，统一处理保存和刷新
- 新增 `resetAddForm()`，统一处理添加后清空表单
- 新增 `getDeletedItems()`，统一找出被删除任务
- 新增 `deleteTodoByIndex()`，统一处理单条删除
- 新增 `saveEditedTodo()`，统一处理编辑保存
- 新增 `setQuickDueDate()`，统一处理快速日期按钮
- `renderTodos()` 增加了关键说明注释
- V1 到 V14 的已有功能仍然保留

V15 仍然没有做这些内容：

- 没有增加新业务功能
- 没有重新设计页面
- 没有做复杂工程框架
- 没有做云端同步
- 没有做后端服务器

当前版本让项目从“结构拆分清楚”继续升级为“功能代码更容易阅读和维护”。

## V16 计划

V16 的目标是：继续整理 `script.js`，把 `renderTodos()` 这个大函数拆成更小、更清楚的函数。

V16 暂定做四组改进：

第一组：拆分统计更新

- 把任务总数、已完成数、未完成数的计算整理成 `getTodoStats()`
- 把页面顶部统计和按钮状态更新整理成 `updateStats()`
- 让统计逻辑不再塞在 `renderTodos()` 里面

第二组：拆分可见任务计算

- 把状态筛选整理成 `todoMatchesStatus()`
- 把标签筛选整理成 `todoMatchesTag()`
- 把可见任务列表整理成 `getVisibleTodos()`
- 让筛选、搜索、排序的顺序更清楚

第三组：拆分页面渲染

- 把空列表提示整理成 `updateEmptyMessage()`
- 把编辑状态任务整理成 `renderEditTodo()`
- 把普通状态任务整理成 `renderNormalTodo()`
- 把单条任务渲染入口整理成 `renderTodoItem()`

第四组：保持功能稳定

- 不改变页面主要外观
- 不增加新业务功能
- 不删除 V1 到 V15 的已有功能
- 让整理后的 `renderTodos()` 只负责调度

V16 暂时不做这些内容：

- 不增加新业务功能
- 不重新设计页面
- 不做复杂工程框架
- 不做云端同步
- 不做后端服务器

V16 的重点是让页面渲染逻辑更清楚，让后续修改列表显示时更容易。

## V16 当前完成情况

目前 V16 已经完成了渲染逻辑拆分。

V16 已经完成的内容包括：

- 新增 `getTodoStats()`，统一计算任务统计
- 新增 `updateStats()`，统一更新统计文字和按钮状态
- 新增 `updateFilterButtons()`，统一更新筛选按钮选中状态
- 新增 `todoMatchesStatus()`，统一判断任务是否符合状态筛选
- 新增 `todoMatchesTag()`，统一判断任务是否符合标签筛选
- 新增 `getVisibleTodos()`，统一计算当前应该显示哪些任务
- 新增 `updateEmptyMessage()`，统一更新空列表提示
- 新增 `renderEditTodo()`，专门渲染编辑状态任务
- 新增 `renderNormalTodo()`，专门渲染普通状态任务
- 新增 `renderTodoItem()`，统一决定渲染编辑状态还是普通状态
- `renderTodos()` 已经瘦身为调度函数
- 导出备份里的版本号已更新为 V16
- V1 到 V15 的已有功能仍然保留

V16 仍然没有做这些内容：

- 没有增加新业务功能
- 没有重新设计页面
- 没有做复杂工程框架
- 没有做云端同步
- 没有做后端服务器

当前版本让 `script.js` 的渲染部分更清楚，后续继续改任务卡片会更容易。

## V17 计划

V17 的目标是：在功能已经够用的基础上，优化前端视觉，让页面更像一个正式小工具。

V17 暂定做四组改进：

第一组：整体视觉

- 优化页面背景
- 增加标题说明区
- 调整页面最大宽度
- 让页面整体更现代、更清爽

第二组：输入区和管理区

- 优化输入框圆角、边框和聚焦效果
- 优化主按钮视觉层级
- 优化输入卡片和管理卡片的阴影、透明度和圆角
- 保持原有功能位置不变

第三组：任务卡片

- 优化任务卡片圆角、阴影和悬停效果
- 给任务卡片增加按优先级区分的左侧颜色线
- 优化标签和优先级标记的视觉样式
- 优化备注区域的层次感

第四组：移动端细节

- 优化手机屏幕上的标题、卡片和按钮显示
- 保持手机端仍然可用
- 不增加新业务功能
- 保留 V1 到 V16 的所有功能

V17 暂时不做这些内容：

- 不增加新任务功能
- 不改数据结构
- 不做复杂动画
- 不做服务器
- 不发布网站

V17 的重点是：先把本地小工具的视觉质量提升一档，再考虑部署上线。

## V17 当前完成情况

目前 V17 已经完成了前端视觉优化。

V17 已经完成的内容包括：

- 页面增加了标题说明区
- 页面背景改为更柔和的渐变背景
- 页面最大宽度从 720px 调整为 860px
- 输入区和管理区卡片圆角、阴影和透明度已优化
- 输入框和选择框的聚焦效果已优化
- 主添加按钮改为渐变样式
- 普通按钮增加了悬停反馈
- 筛选按钮区域改为更清楚的分段样式
- 任务卡片圆角、阴影和悬停效果已优化
- 任务卡片新增按优先级区分的左侧颜色线
- 标签和优先级标记视觉已优化
- 备注区域视觉层次已优化
- 日期统计和保存统计区域视觉已优化
- 数据管理按钮视觉已优化
- 手机端标题、卡片和按钮显示已优化
- 导出备份里的版本号已更新为 V17
- V1 到 V16 的已有功能仍然保留

V17 仍然没有做这些内容：

- 没有增加新任务功能
- 没有改变数据结构
- 没有做复杂动画
- 没有做服务器
- 没有发布网站

当前版本让项目从“功能完整的本地工具”继续升级为“视觉更接近正式产品的本地工具”。

## 部署上线记录

项目已通过 GitHub Pages 部署上线。

GitHub 仓库地址：

```text
https://github.com/wangyegzhm/simple-todo-list
```

线上访问地址：

```text
https://wangyegzhm.github.io/simple-todo-list/
```

当前线上版本来自 V17 视觉优化版。

## 下一步计划

接下来可以按下面的顺序推进：

1. 检查 V17 视觉优化后，页面显示和已有功能是否仍然正常。
2. 如果视觉满意，可以准备部署上线。
3. 部署前确认 Git 状态干净，并创建 GitHub 备份。
4. 如果后续需要多设备同步，再考虑服务器和数据库。

这个项目的目标不是做一个复杂的任务管理系统，而是先完成一个小而完整的待办清单工具。
