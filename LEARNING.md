# 学习总结：简单待办清单

这份文档用来记录这次做“简单待办清单”的学习收获。

这个项目从一个最简单的页面开始，一步一步变成了一个可以在浏览器里使用的小工具。

## 从 V1 到 V16 做了什么

### V1：完成最基础的待办清单

V1 先完成了待办清单最核心的功能：

- 可以添加待办事项
- 可以显示待办列表
- 可以勾选完成
- 可以删除待办事项
- 可以按 Enter 添加待办事项
- 可以用 localStorage 保存数据，刷新后内容还在

这一版让项目从“静态页面”变成了“可以真正使用的小工具”。

### V2：让页面更清楚、更好用

V2 主要优化了页面体验：

- 页面整体居中
- 背景颜色更柔和
- 输入框和按钮更好看
- 待办事项像一张张小卡片
- 列表为空时显示提示
- 显示还有几件待办未完成
- 可以一键清除已完成事项

这一版让工具看起来更舒服，也更容易知道当前还有多少事情没做。

### V3：增加任务筛选

V3 增加了筛选功能：

- 全部：显示所有任务
- 未完成：只显示还没完成的任务
- 已完成：只显示已经完成的任务

筛选只是改变页面上看到的内容，不会删除任务。

这一版让用户可以按状态查看任务。

### V4：增加编辑任务文字

V4 增加了编辑功能：

- 每条待办事项都有“编辑”按钮
- 点击“编辑”后，可以修改任务文字
- 点击“保存”后，会更新任务文字
- 点击“取消”后，会保留原来的文字
- 修改后的内容会保存到 localStorage

这一版解决了“任务写错了，只能删除再重新添加”的问题。

### V5：增加创建时间

V5 给每条任务增加了创建时间：

- 新增任务时，会自动记录当前时间
- 每条任务会显示“创建于 14:32”这样的文字
- 刷新页面后，创建时间仍然保留
- 编辑任务文字时，不会改变创建时间

这一版让一条任务不再只有文字和完成状态，还多了“什么时候创建”的信息。

### V6：增加截止日期

V6 给每条任务增加了截止日期：

- 添加任务时，可以选择截止日期
- 每条任务会显示截止日期
- 没有选择日期时，会显示“截止：未设置”
- 编辑任务时，可以修改截止日期
- 刷新页面后，截止日期仍然保留

这一版让任务多了“最好什么时候完成”的信息，但还没有做提醒、日历或排序。

### V7：增强日期管理

V7 不再只做一个小变化，而是围绕“日期管理”做了一组改进：

- 可以选择“原来的顺序”
- 可以选择“截止日期优先”
- 没有截止日期的任务会排在后面
- 今天截止的未完成任务会显示“今天截止”
- 已经逾期的未完成任务会显示“已逾期”
- 页面会显示今天截止几件、已逾期几件

这一版开始学习“排序”和“根据日期判断状态”。同一批任务数据没有变，但页面可以用更清楚的方式展示它们。

### V8：增强输入体验和操作反馈

V8 围绕“用户操作更清楚、更稳”做了一组改进：

- 空输入时，会提示用户先输入任务
- 任务文字最多 60 字
- 输入框下方会显示当前字数
- 添加成功后会显示短提示
- 编辑保存成功后会显示短提示
- 浏览器保存失败时会显示兜底提示
- 清除已完成前会先确认，避免误删

这一版开始学习“表单验证”和“操作反馈”。简单说，就是用户操作不合适时要告诉他哪里不对，用户操作成功时也要给一个清楚回应。

### V9：增强批量操作

V9 围绕“一次处理多条任务”做了一组改进：

- 可以一键全部完成
- 可以一键全部取消完成
- 可以继续一键清除已完成
- 页面会显示总任务数、已完成数、未完成数
- 没有任务时，批量操作按钮会自动不可用
- 批量操作后会显示短提示

这一版开始学习“批量操作”。简单说，就是不用一条一条改，而是一次改变很多条任务的状态。

### V10：增加搜索与快速定位

V10 围绕“任务变多以后怎么快速找到”做了一组改进：

- 增加搜索框
- 输入关键词后，只显示匹配的任务
- 搜索可以和“全部 / 未完成 / 已完成”筛选一起工作
- 搜索可以和排序方式一起工作
- 搜索时会显示找到几条结果
- 可以一键清空搜索
- 搜索不到结果时，会显示清楚提示

这一版开始学习“搜索”和“组合条件”。简单说，就是先按状态筛选，再按关键词搜索，最后再排序。

### V11：任务管理增强版

V11 不只增加一个功能，而是一次完成三组相关升级。

第一组是优先级管理：

- 添加任务时可以选择低、中、高优先级
- 每条任务会显示优先级标记
- 不同优先级用不同颜色区分
- 编辑任务时可以修改优先级
- 可以按优先级排序

第二组是任务备注：

- 添加任务时可以写备注
- 每条任务可以显示备注
- 没有备注时不显示空区域
- 编辑任务时可以修改备注
- 搜索时也可以匹配备注

第三组是界面整理：

- 添加任务区域更集中
- 统计、批量、筛选、搜索、排序放在管理区域
- 手机屏幕上仍然能正常使用

这一版开始学习“一个版本可以包含多个相关功能包”。功能变多以后，更需要保持它们围绕同一个目标，而不是随便堆在页面上。

### V12：标签、快速日期和数据管理

V12 继续使用“更大的版本包”方式，一次完成三组相关升级。

第一组是标签管理：

- 添加任务时可以写标签
- 每条任务可以显示标签
- 编辑任务时可以修改标签
- 可以按标签筛选任务
- 搜索时也可以匹配标签

第二组是快速日期：

- 点击“今天”可以快速设置今天截止
- 点击“明天”可以快速设置明天截止
- 点击“清除日期”可以快速清空添加区的截止日期
- 手动选择日期的能力仍然保留

第三组是数据管理：

- 页面会显示浏览器保存了多少条任务
- 可以一键清空全部任务
- 清空全部前需要二次确认，减少误删风险
- 清空全部后，标签筛选会自动回到“全部标签”

这一版开始学习“分类”和“数据管理”。简单说，标签让任务更好找，快速日期让添加更快，清空全部让工具有了更明确的数据管理入口。

### V13：数据安全和备份

V13 继续推进“一个版本包含多组相关改进”的节奏，这次主题是数据安全。

第一组是撤销删除：

- 删除单条任务后可以撤销
- 清除已完成任务后可以撤销
- 清空全部任务后可以撤销
- 撤销提示会保留一小段时间

第二组是导出备份：

- 可以把当前任务导出成 JSON 文件
- 备份文件里保存了任务文字、完成状态、创建时间、截止日期、优先级、标签和备注
- 没有任务时，导出按钮不可用

第三组是导入备份：

- 可以选择以前导出的 JSON 文件
- 导入前会检查文件里有没有有效任务
- 导入会替换当前列表，所以会先确认
- 导入后会保存到浏览器里

第四组是恢复示例数据：

- 可以一键恢复三条示例任务
- 恢复示例数据会替换当前列表，所以会先确认
- 恢复后，筛选、搜索和排序会回到默认状态

这一版开始学习“备份”和“误删保护”。简单说，工具不只是能添加任务，也要考虑用户不小心删错东西时怎么办。

### V14：拆分项目文件

V14 没有急着继续加新功能，而是整理项目结构。

前面的版本里，页面结构、页面样式和功能代码都写在 `index.html` 里。

这样一开始很方便，因为只有一个文件，容易打开，也容易理解。

但功能越来越多以后，一个文件会变得很长，找东西会越来越慢。

所以 V14 把项目拆成三个文件：

- `index.html`：负责页面结构
- `style.css`：负责页面样式
- `script.js`：负责页面功能

拆分后，功能没有变，但项目更清楚。

比如：

- 要改页面文字，去看 `index.html`
- 要改按钮颜色，去看 `style.css`
- 要改添加、删除、搜索、备份功能，去看 `script.js`

这一版开始学习“项目结构”。简单说，项目变大以后，不只要会加功能，也要会整理文件。

### V15：整理功能代码

V15 没有继续增加新的页面功能，而是整理 `script.js`。

项目越做越大以后，代码不只要“能用”，还要“容易看、容易改”。

所以 V15 做了几件事：

- 给 `script.js` 增加分区注释
- 把页面元素、数据状态、工具函数、页面渲染、用户操作、事件绑定分开
- 把重复的“保存并刷新”整理成 `saveAndRender()`
- 把添加后清空输入区整理成 `resetAddForm()`
- 把删除任务、编辑保存、快速日期整理成更清楚的小函数
- 给 `renderTodos()` 等关键区域增加新手注释

这一版开始学习“维护代码”。简单说，项目不是功能越多越好，也要让未来的自己能看懂、能继续改。

### V16：拆分渲染函数

V16 继续整理 `script.js`，这次重点整理 `renderTodos()`。

`renderTodos()` 是这个项目里很重要的函数。

它的作用是：

```text
根据当前 todos 数据，把页面上的任务列表重新画出来
```

前面的版本里，它做的事情越来越多：

- 更新统计数字
- 判断哪些任务应该显示
- 显示空列表提示
- 画编辑状态的任务
- 画普通状态的任务

V16 把这些事情拆成了几个更小的函数：

- `getTodoStats()`：计算任务总数、已完成数、未完成数
- `updateStats()`：更新页面上的统计文字和按钮状态
- `getVisibleTodos()`：根据筛选、搜索、标签和排序，算出要显示的任务
- `updateEmptyMessage()`：决定空列表时显示什么提示
- `renderEditTodo()`：画编辑状态的任务
- `renderNormalTodo()`：画普通状态的任务
- `renderTodoItem()`：决定一条任务应该用编辑状态还是普通状态来显示

这一版开始学习“渲染拆分”。简单说，一个大函数太忙了，就把它负责的事情分给几个小函数。

## HTML、CSS、JavaScript 分别负责什么

### HTML 负责页面有什么

HTML 像页面的骨架。

在这个项目里，HTML 负责放这些内容：

- 标题
- 输入框
- 添加按钮
- 未完成数量文字
- 清除已完成按钮
- 筛选按钮
- 待办列表
- 空列表提示

简单理解：HTML 决定页面上有哪些东西。

### CSS 负责页面长什么样

CSS 像页面的衣服和排版。

在这个项目里，CSS 负责：

- 背景颜色
- 页面宽度
- 内容居中
- 标题大小
- 输入框样式
- 按钮颜色
- 待办事项卡片样式
- 已完成文字的删除线
- 手机屏幕上的显示方式

简单理解：CSS 决定页面看起来漂不漂亮、清不清楚。

### JavaScript 负责页面能做什么

JavaScript 像页面的大脑。

在这个项目里，JavaScript 负责：

- 添加任务
- 删除任务
- 勾选完成
- 取消完成
- 保存到 localStorage
- 从 localStorage 读取数据
- 更新未完成数量
- 显示或隐藏空列表提示
- 按状态筛选任务
- 编辑任务文字
- 记录创建时间
- 设置截止日期
- 按截止日期排序
- 统计今天截止和已逾期任务
- 检查输入内容
- 显示操作反馈
- 处理保存失败
- 批量更新任务状态
- 根据任务数量启用或禁用按钮
- 根据关键词搜索任务
- 把筛选、搜索、排序组合在一起
- 设置和显示优先级
- 添加和编辑任务备注
- 按优先级排序
- 把页面分成输入区和管理区

简单理解：JavaScript 决定页面能不能动、能不能响应用户操作。

## todos 是什么

`todos` 是一个数组。

数组可以理解成一个列表。这个列表里保存了所有待办事项。

每一条待办事项大概长这样：

```js
{
  text: '完成作业',
  completed: false,
  createdAt: '14:32',
  dueDate: '2026-08-30',
  priority: 'high',
  note: '先完成数学部分'
}
```

其中：

- `text` 表示任务文字
- `completed` 表示任务有没有完成
- `createdAt` 表示任务是什么时候创建的
- `dueDate` 表示任务的截止日期
- `priority` 表示任务优先级
- `note` 表示任务备注

如果 `completed` 是 `false`，表示还没完成。

如果 `completed` 是 `true`，表示已经完成。

如果 `dueDate` 是空的，表示这条任务暂时没有设置截止日期。

如果 `note` 是空的，表示这条任务暂时没有备注。

## renderTodos() 是什么

`renderTodos()` 是一个函数。

函数可以理解成“一组提前写好的操作步骤”。

`renderTodos()` 的作用是：根据当前的 `todos` 数据，重新显示页面上的待办列表。

当用户添加、删除、勾选、筛选、排序、编辑任务后，页面都需要重新显示一次，这时就会用到 `renderTodos()`。

简单理解：`renderTodos()` 负责把数据画到页面上。

## localStorage 是什么

`localStorage` 是浏览器提供的本地保存功能。

它可以把数据存在用户自己的浏览器里。

在这个项目里，待办事项会保存到 `localStorage`。所以刷新页面后，之前添加的任务还会保留。

需要注意的是，`localStorage` 不是后端服务器，也不是账号系统。它只保存在当前这个浏览器里。

## addEventListener 是什么

`addEventListener` 是用来监听用户操作的。

比如：

- 用户点击按钮
- 用户按下键盘
- 用户勾选复选框

都可以用 `addEventListener` 来监听。

简单理解：`addEventListener` 就是在告诉网页：“当用户做了某个动作，就执行某段代码。”

## completed、currentFilter、currentSort、editingIndex、createdAt、dueDate、priority、note 分别表示什么

### completed

`completed` 表示一条待办事项有没有完成。

- `completed: false` 表示未完成
- `completed: true` 表示已完成

页面会根据它决定复选框是否勾选，以及任务文字是否显示删除线。

### currentFilter

`currentFilter` 表示当前选择了哪个筛选状态。

它可能是：

- `all`：显示全部任务
- `active`：只显示未完成任务
- `completed`：只显示已完成任务

简单理解：`currentFilter` 记录用户现在想看哪一类任务。

### currentSort

`currentSort` 表示当前选择了哪种排序方式。

它可能是：

- `original`：按原来的顺序显示
- `dueDate`：按截止日期优先显示
- `priority`：按优先级优先显示

简单理解：`currentSort` 记录用户现在想按什么顺序看任务。

### editingIndex

`editingIndex` 表示当前正在编辑哪一条任务。

每条任务在 `todos` 数组里都有自己的位置，这个位置叫 index。

如果 `editingIndex` 是一个数字，表示正在编辑这个位置上的任务。

如果 `editingIndex` 是 `null`，表示现在没有任务正在编辑。

### createdAt

`createdAt` 表示任务创建时间。

比如：

```js
createdAt: '14:32'
```

简单理解：`createdAt` 记录这条任务是什么时候被添加进来的。

### dueDate

`dueDate` 表示任务的截止日期。

比如：

```js
dueDate: '2026-08-30'
```

如果 `dueDate` 是空字符串，表示用户没有给这条任务设置截止日期。

简单理解：`dueDate` 记录这条任务最好什么时候完成。

### priority

`priority` 表示任务优先级。

它可能是：

- `low`：低优先级
- `medium`：中优先级
- `high`：高优先级

简单理解：`priority` 记录这条任务重要不重要、着不着急。

### note

`note` 表示任务备注。

比如：

```js
note: '先完成数学部分'
```

备注可以写补充说明，也可以留空。

简单理解：`note` 用来记录任务文字之外的补充信息。

## 排序和筛选有什么区别

筛选是决定“哪些任务显示出来”。

比如：

- 全部：显示所有任务
- 未完成：只显示未完成任务
- 已完成：只显示已完成任务

排序是决定“显示出来的任务按什么顺序排列”。

比如：

- 原来的顺序：按添加顺序显示
- 截止日期优先：日期更早的任务排在前面

简单理解：

```text
筛选 = 看哪一些
排序 = 先看哪一条
```

## 搜索是什么

搜索就是根据用户输入的关键词，从任务列表里找出匹配的任务。

比如有这些任务：

- 买牛奶
- 写作业
- 买面包

如果搜索：

```text
买
```

页面就只显示：

- 买牛奶
- 买面包

在这个项目里，搜索会匹配任务文字和备注，不匹配创建时间和截止日期。

## 组合条件是什么

组合条件就是多个规则一起生效。

比如 V10 里，页面显示任务时会经过几步：

```text
先按状态筛选
再按关键词搜索
最后按排序方式排列
```

例子：

```text
筛选：未完成
搜索：买
排序：截止日期优先
```

意思是：

```text
先找未完成任务
再从里面找包含“买”的任务
最后按截止日期排列
```

简单理解：

```text
筛选、搜索、排序可以一起工作
```

## 表单验证和操作反馈是什么

表单验证就是在用户提交内容前，先检查内容是否合理。

比如这个项目里：

- 输入框为空时，不添加任务
- 任务文字最多 60 字
- 编辑任务时，不能把任务文字保存为空

操作反馈就是在用户做完一件事后，页面给一个回应。

比如：

- 添加成功后显示“已添加”
- 编辑成功后显示“修改已保存”
- 没有已完成任务时，点击“清除已完成”会显示提示
- 保存失败时，告诉用户浏览器没有成功保存

简单理解：

```text
表单验证 = 操作前检查
操作反馈 = 操作后告诉用户结果
```

## 批量操作是什么

批量操作就是一次处理多条数据。

比如这个项目里：

- 点击“全部完成”，会把所有未完成任务都变成已完成
- 点击“全部取消完成”，会把所有已完成任务都变回未完成
- 点击“清除已完成”，会删除所有已完成任务

批量操作通常会和统计信息一起出现。

比如：

```text
共 5 件，已完成 2 件，未完成 3 件
```

这样用户在点击批量按钮前，就能大概知道这次操作会影响多少任务。

简单理解：

```text
单条操作 = 处理一条任务
批量操作 = 一次处理很多条任务
```

## 优先级和备注是什么

优先级是用来表示任务重要程度的字段。

比如：

```text
高优先级：今天必须做
中优先级：正常处理
低优先级：有空再做
```

备注是任务的补充说明。

比如任务文字是：

```text
写作业
```

备注可以是：

```text
先写数学，再写英语
```

简单理解：

```text
priority = 任务重要程度
note = 任务补充说明
```

## 标签是什么

标签是用来给任务做简单分类的文字。

比如：

```text
学习
工作
生活
整理
```

如果一条任务是：

```text
复习英语单词
```

它的标签可以是：

```text
学习
```

这样以后任务变多时，就可以只看“学习”这一类任务。

简单理解：

```text
tag = 任务分类小标签
```

## 快速日期按钮是什么

快速日期按钮是为了减少重复操作。

如果每次都手动打开日期选择器，再选今天或明天，会有一点慢。

所以 V12 增加了：

- 今天
- 明天
- 清除日期

点击一次就能改添加区的截止日期。

简单理解：

```text
快速按钮 = 把常用操作变成一次点击
```

## 数据管理是什么

数据管理就是让用户知道数据在哪里、多少条，以及怎么处理这些数据。

这个项目的数据保存在浏览器的 localStorage 里。

V12 增加了两个和数据管理有关的能力：

- 显示浏览器已保存多少条任务
- 可以清空全部任务

因为“清空全部”会删除很多内容，所以它需要二次确认。

简单理解：

```text
数据管理 = 看清数据 + 安全处理数据
```

## 撤销删除是什么

撤销删除就是给用户一次“反悔”的机会。

比如用户点了删除，页面不会只默默删除，而是提示：

```text
已删除，可撤销
```

如果用户马上发现删错了，就可以点“撤销”，把刚刚删除的任务恢复回来。

简单理解：

```text
撤销删除 = 给误操作留一个后悔按钮
```

## 导出和导入是什么

导出就是把浏览器里的任务保存成一个文件。

导入就是把这个文件重新读回网页里。

这个项目使用的是 JSON 文件。

JSON 是一种常见的数据格式，看起来有点像这样：

```text
{
  "text": "完成作业",
  "completed": false
}
```

简单理解：

```text
导出 = 从网页保存到文件
导入 = 从文件恢复到网页
JSON = 一种保存数据的文件格式
```

## 代码分区是什么

代码分区就是给代码分大段落。

就像写文章会有标题：

```text
第一章
第二章
第三章
```

代码也可以有自己的大标题。

比如 V15 里，`script.js` 分成了：

- 页面元素
- 固定配置和页面状态
- 初始数据
- 保存与提示
- 数据小工具
- 统计、筛选、搜索、排序
- 页面渲染
- 用户操作
- 事件绑定
- 页面启动

这样以后想找某段功能，就不用从头到尾乱翻。

简单理解：

```text
代码分区 = 给代码加目录感
```

## 小函数是什么

小函数就是把一段重复或独立的动作取一个名字。

比如以前很多地方都要做：

```text
保存数据
刷新页面
显示提示
```

V15 把它整理成：

```text
saveAndRender()
```

以后看到这个名字，就知道它大概是在“保存并刷新”。

简单理解：

```text
小函数 = 把一小段动作装进一个有名字的小盒子
```

## 渲染是什么

渲染就是把数据变成页面上看得见的内容。

比如程序里有一条数据：

```text
任务：买牛奶
状态：未完成
标签：生活
```

渲染以后，用户在页面上看到的就是一张任务卡片：

```text
[ ] 生活 买牛奶
```

在这个项目里，`renderTodos()` 就是负责渲染任务列表的核心函数。

V16 把它拆小，是因为它原来负责的事情太多。

简单理解：

```text
渲染 = 把数据画到页面上
```

## 更大的版本包是什么

前面的版本通常一次只做一个功能，后来升级成一个主题包。

V11 开始，一个版本可以包含多个相关主题包。

比如 V11 同时做了：

- 优先级管理
- 任务备注
- 界面整理

但它们都围绕同一个目标：

```text
让待办清单更像一个任务管理工具
```

V12 又同时做了：

- 标签管理
- 快速日期
- 数据管理

它们共同围绕一个目标：

```text
让待办清单更容易整理，也更安全地管理数据
```

V13 又同时做了：

- 撤销删除
- 导出备份
- 导入备份
- 恢复示例数据

它们共同围绕一个目标：

```text
让待办清单的数据更安全
```

V15 做的不是新功能，而是维护性升级：

- 增加代码分区
- 整理重复逻辑
- 补充关键注释
- 保持功能稳定

它的目标是：

```text
让代码更容易看懂，也更容易继续开发
```

V16 继续做维护性升级：

- 拆分统计更新
- 拆分可见任务计算
- 拆分空列表提示
- 拆分编辑状态渲染
- 拆分普通状态渲染

它的目标是：

```text
让 renderTodos() 不再一个函数负责太多事情
```

简单理解：更大的版本包不是乱加功能，而是围绕同一个目标，一次推进多组相关改进。

## 我下一步可以继续学习什么

接下来可以继续学习这些内容：

1. 学习更多 HTML 标签，理解页面结构怎么写。
2. 学习更多 CSS 样式，让页面更好看。
3. 学习 JavaScript 数组，比如 `push`、`filter`、`map`、`splice`。
4. 学习函数，把重复代码整理得更清楚。
5. 学习事件，比如点击、输入、键盘操作。
6. 学习浏览器存储，比如 localStorage 的读取和保存。
7. 学习怎么把一个大功能拆成几个小步骤。
8. 学习怎么检查代码，确认功能有没有符合产品说明。
9. 学习排序逻辑，比如按日期、按优先级排序。
10. 学习怎么把一个版本做成一组相关改进。
11. 学习表单验证，比如必填、长度限制、格式检查。
12. 学习操作反馈，比如成功提示、错误提示、确认提示。
13. 学习批量操作，比如全部完成、全部取消、批量删除。
14. 学习按钮状态，比如什么时候可以点击，什么时候应该不可用。
15. 学习搜索逻辑，比如关键词匹配。
16. 学习组合条件，比如筛选、搜索和排序一起使用。
17. 学习更多数据字段，比如优先级、备注和标签。
18. 学习更大的版本规划，让一次升级包含多个相关功能包。
19. 学习分类思维，比如用标签筛选任务。
20. 学习数据管理思维，比如统计保存数量和清空全部前二次确认。
21. 学习撤销删除，让用户误操作后有机会恢复。
22. 学习导入和导出，理解浏览器数据也可以保存成文件。
23. 学习 JSON，理解一种常见的数据保存格式。
24. 学习代码分区，让长文件更容易阅读。
25. 学习小函数，把重复动作整理成有名字的代码块。
26. 学习维护性升级，理解有些版本不是加功能，而是让项目更健康。
27. 学习渲染，理解数据是怎么变成页面内容的。
28. 学习拆分大函数，让一个复杂函数变成多个清楚的小函数。

这个项目最重要的收获是：一个网页工具可以先从很小的功能开始，再一步一步增加能力。每次只做一点，项目就会越来越完整。
